let state={matches:[],leaderboard:[]}; const $=s=>document.querySelector(s); const fmt=d=>new Intl.DateTimeFormat(undefined,{dateStyle:'medium',timeStyle:'short'}).format(new Date(d));
async function api(path, opts={}){opts.headers={...(opts.headers||{}),'Content-Type':'application/json','x-admin-password':$('#adminPass').value}; const r=await fetch('/.netlify/functions/'+path, opts); const j=await r.json(); if(!r.ok) throw new Error(j.error||'Request failed'); return j;}
async function getState(){const r=await fetch('/.netlify/functions/get-state'); state=await r.json(); render();}
function renderLeaderboard(){const rows=state.leaderboard.map((p,i)=>`<tr><td>${i+1}</td><td>${p.nickname}</td><td><b>${p.points}</b></td><td>${p.predictions}</td></tr>`).join(''); $('#leaderboard').innerHTML=`<table><thead><tr><th>#</th><th>Name</th><th>Pts</th><th>Picks</th></tr></thead><tbody>${rows||'<tr><td colspan="4">No players yet</td></tr>'}</tbody></table>`;}
function render(){renderLeaderboard(); $('#adminMatches').innerHTML=state.matches.map(m=>`<div class="match"><b>${m.home} vs ${m.away}</b><p class="small">${m.round} • ${fmt(m.kickoff)} • ID ${m.id}</p><div class="row"><input type="number" min="0" value="${m.home_score??''}" placeholder="${m.home}" data-h="${m.id}"><input type="number" min="0" value="${m.away_score??''}" placeholder="${m.away}" data-a="${m.id}"><button data-result="${m.id}">Save result</button></div></div>`).join(''); document.querySelectorAll('[data-result]').forEach(btn=>btn.onclick=async()=>{try{const id=btn.dataset.result; const h=document.querySelector(`[data-h="${id}"]`).value; const a=document.querySelector(`[data-a="${id}"]`).value; await api('admin-set-result',{method:'POST',body:JSON.stringify({match_id:id,home_score:h===''?null:Number(h),away_score:a===''?null:Number(a)})}); $('#adminStatus').innerHTML='<span class="ok">Result saved.</span>'; await getState();}catch(e){$('#adminStatus').innerHTML=`<span class="bad">${e.message}</span>`;}});}
$('#saveMatch').onclick=async()=>{try{const body={id:$('#id').value||undefined,round:$('#round').value,group_name:$('#group').value,home:$('#home').value,away:$('#away').value,kickoff:$('#kickoff').value}; await api('admin-save-match',{method:'POST',body:JSON.stringify(body)}); $('#adminStatus').innerHTML='<span class="ok">Match saved.</span>'; ['id','round','group','home','away','kickoff'].forEach(x=>$('#'+x).value=''); await getState();}catch(e){$('#adminStatus').innerHTML=`<span class="bad">${e.message}</span>`;}};
getState();
